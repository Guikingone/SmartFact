App\Model\Bills:
    type: document
    collection: _smartfact_bills
    fields:
        id:
            type: id
            id: true
            strategy: AUTO
        date:
            type: date
            nullable: false
        total:
            type: float
            nullable: false
        send:
            type: boolean
            nullable: false
        createdAt:
            type: date
            nullable: false
        sendAt:
            type: date
            nullable: false
        file:
            type: string
            length: 150
            nullable: false
        tva:
            type: boolean
            nullable: false

    referenceMany:
        clients:
            targetDocument: App\Model\Clients
            inversedBy: bills

    referenceOne:
        user:
            targetDocument: App\Model\User
            inversedBy: bills
